# Weight: how close to you to a certain point

Weight: how close to you to a certain point

## Usage

``` r
close_to(
  board,
  i_star = ceiling(dim(board)[1]/2),
  j_star = ceiling(dim(board)[1]/2)
)
```
